# secrets and local machine config
.env
.mcp.json
.claude/settings.local.json

# personal "context-mode" editor tooling (separate local tool, not part of Rover)
.claude/settings.json
.vscode/
.github/hooks/

# runtime state (temp files, not source)
data/*.json
data/*.sqlite3
data/.ai_prompts/
.rover/
.menisik/
.stream_partials/
.claude/scheduled_tasks.lock

# output / review
pending_review/
builds.html
runs/
artifacts/
pr264_fix/
worktrees/

# logs
logs/

# python
__pycache__/
.pytest_cache/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.venv/
venv/
.codex/

# cross-platform editor / filesystem residue
*:Zone.Identifier

# build artifact, do not track
*.zip

# internal planning scratch — kept locally, not part of the public repo
newplan.md
docs/validation/
